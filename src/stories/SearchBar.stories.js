import SearchBar from '../components/SearchBar';

export default {
  title: 'Search Bar',
  component: SearchBar,
};

export const Template = () => ({
  components: { SearchBar },
  template:
    '<search-bar />'
});






